#include <unordered_map>
#include <cstring>
#include "src/Value.h"

using namespace h7l;

//static std::unordered_map<String, std::function<void(Value&)>> s_PrimitiveMap =
//{
//        {TYPE_ID_INT, [](){}}
//};

void Value::setPrimitiveValue(Type *type, char* ptr){
    //memcpy(&as_int, ptr, sizeof(int));
//#define _APPLY(t, key)\

}
